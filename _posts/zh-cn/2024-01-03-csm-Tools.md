---
title: CSM调试与开发工具
author: nevstop
date: 2024-01-03
layout: post
lang: zh-cn
page_id: CSM-Tools
permalink: /csm-tools
toc: true
---

CSM框架提供了一系列调试和开发工具，帮助开发者更高效地开发、调试和维护基于CSM的应用。这些工具涵盖了从代码编写、运行时调试到接口管理的各个方面。

## 工具概述

CSM的工具可以分为以下几类：

- **运行时调试工具**: 实时监控CSM模块的运行状态
- **开发辅助工具**: 提高开发效率的代码生成和管理工具
- **接口管理工具**: 浏览和管理CSM模块的接口
- **示例浏览工具**: 快速查找和学习CSM示例

所有的运行时调试工具都基于CSM全局日志系统实现，可以实时监控系统运行状态。关于全局日志的详细信息，请参考[CSM全局日志系统](./2024-01-04-csm-global-log.html)。

## 工具入口

CSM工具提供了两个便捷的入口：

### 1. LabVIEW菜单入口

在LabVIEW的菜单栏中，选择 `Tools` -> `Communicable State Machine(CSM)` -> `Open CSM Tool Launcher...`

![CSM Tool Entry in Menu](https://nevstop-lab.github.io/CSM-Wiki/assets/img/Tools/Debug%20Tool%20Entry%20in%20Tool%20Menu.png)

### 2. 函数面板入口

在LabVIEW的Communicable State Machine(CSM)函数面板中，选择`CSM Tools`

![CSM Tool Entry in Palette](https://nevstop-lab.github.io/CSM-Wiki/assets/img/Tools/Debug%20Tool%20Entry%20in%20Palette.png)

### 工具选择器

通过以上任一入口，都会打开CSM工具选择器，从中可以选择需要使用的工具：

![CSM Tool Selector](https://nevstop-lab.github.io/CSM-Wiki/assets/img/Tools/Tool%20Selector.png)

## 运行时调试工具

运行时调试工具用于监控CSM应用的实时运行状态，这些工具都基于全局日志系统实现。

### CSM Running Log Window（运行日志窗口）

运行日志窗口实时显示CSM系统的运行日志，记录模块的状态变化、消息通讯、广播等所有事件。

![CSM Running Log Window](https://nevstop-lab.github.io/CSM-Wiki/assets/img/Tools/tool-RunningLogWindow.png)

**主要功能**：
- **实时日志显示**: 以时间顺序显示所有CSM事件
- **日志过滤**: 可以按模块名称、事件类型等条件过滤日志
- **日志搜索**: 快速查找特定的日志记录
- **日志导出**: 将日志保存为文件，用于事后分析
- **性能监控**: 显示日志产生速度，监控系统负载

**适用场景**：
- 追踪消息流动路径
- 分析模块间的交互时序
- 排查状态机的运行问题
- 记录系统运行历史

**使用技巧**：
- 使用过滤功能聚焦于特定模块或事件类型
- 利用周期性日志折叠功能，提高日志可读性
- 在系统负载高时，自动调整日志处理等级
- 导出日志文件用于离线分析

### CSM State Dashboard Window（状态仪表板）

状态仪表板以图形化方式显示所有CSM模块的当前状态，提供系统运行状态的一目了然的视图。

![CSM State Dashboard Window](https://nevstop-lab.github.io/CSM-Wiki/assets/img/Tools/tool-DashboardWindow.png)

**主要功能**：
- **模块列表**: 显示所有活动的CSM模块
- **当前状态**: 实时显示每个模块正在执行的状态
- **状态历史**: 保留最近的状态变化历史
- **模块筛选**: 支持按名称或类型筛选模块
- **颜色标识**: 不同状态类型使用不同颜色区分

**适用场景**：
- 快速查看系统整体运行状态
- 监控多个模块的协同工作
- 发现卡死或异常的模块
- 验证状态机的状态转换

**状态颜色说明**：
- 绿色：正常执行状态
- 黄色：等待或空闲状态
- 红色：错误状态
- 蓝色：外部调用状态

### CSM Table Log Window（表格日志窗口）

表格日志窗口以表格形式记录CSM模块的状态变化，每个模块占一列，每次状态变化产生一行新记录。这种展示方式特别适合分析并行运行的多个模块之间的状态对应关系。

![CSM Table Log Window](https://nevstop-lab.github.io/CSM-Wiki/assets/img/Tools/tool-TableLogWindow.png)

**主要功能**：
- **并行状态对比**: 横向对比不同模块的状态变化
- **时序关系**: 纵向查看状态变化的时间顺序
- **状态同步**: 发现模块间的状态同步关系
- **事件关联**: 分析因果关系和触发链

**适用场景**：
- 分析多模块协同工作的时序
- 发现并发问题和竞态条件
- 验证模块间的同步机制
- 追踪消息传递链

**使用技巧**：
- 关注同一行中多个模块的状态变化，理解模块间的交互
- 查看某一模块的列，了解该模块的状态变化序列
- 使用颜色标识快速识别关键事件
- 结合运行日志窗口进行深入分析

## 开发辅助工具

开发辅助工具帮助开发者提高代码编写效率，自动化常见的开发任务。

### CSM Debug Console（调试控制台）

调试控制台提供了一个交互式界面，用于测试CSM模块的API接口。

**主要功能**：
1. **模块选择**: 选择需要测试的CSM模块
2. **API扫描**: 自动扫描模块的所有API接口
3. **消息发送**: 发送同步或异步消息到模块
4. **结果查看**: 查看消息执行的返回值
5. **日志监控**: 查看相关的日志信息
6. **脚本执行**: 保存和执行测试脚本

**适用场景**：
- 单元测试CSM模块的接口
- 快速验证模块功能
- 调试模块的消息处理逻辑
- 执行自动化测试脚本

**使用流程**：
1. 启动要测试的CSM模块
2. 在调试控制台中选择该模块
3. 扫描模块的API接口
4. 选择要测试的API并输入参数
5. 发送消息并查看结果
6. 根据需要调整参数重复测试

### Add VI Reference Case to CSMs（添加VI引用分支）

这个工具用于为CSM模块批量添加"VI Reference"分支，方便外部获取VI引用。

**工作流程**：
1. 扫描lvproj目录下的所有VI
2. 识别哪些VI是CSM模块
3. 列出所有找到的CSM模块
4. 用户选择需要添加分支的模块
5. 自动在选定模块中添加"VI Reference"分支

**适用场景**：
- 批量为现有CSM模块添加VI引用访问接口
- 统一模块的接口规范
- 迁移旧版本代码

**注意事项**：
- 操作前建议备份代码
- 确保目标VI未被其他程序打开
- 添加后需要保存VI

### Switch Language Tool（切换语言工具）

切换CSM模块VI中注释的语言，目前支持中文和英文。

**主要功能**：
- 批量切换模块注释的语言
- 支持中英文互换
- 保持代码结构不变

**适用场景**：
- 国际化项目的语言切换
- 团队协作中的语言统一
- 文档本地化

### Fix JKISM Editor RCM Entry（修复JKISM编辑器菜单）

修复JKISM State Editor右键菜单在CSM中不能调用的问题。

**问题描述**：
在某些情况下，JKISM State Editor的右键菜单可能无法在CSM模块中正常使用。

**解决方法**：
运行此工具可以修复右键菜单注册，恢复State Editor的功能。

### Create CSM Palette at Root（创建CSM函数面板）

在项目根目录生成CSM模块的函数选板，方便在项目中快速访问CSM函数。

**适用场景**：
- 在大型项目中快速访问CSM函数
- 自定义项目的函数面板布局
- 提高开发效率

### Remove All CSM Bookmarks（移除CSM书签）

移除CSM模块中所有的书签标记。

**适用场景**：
- 清理开发过程中添加的临时书签
- 准备代码发布
- 统一代码规范

## 接口管理工具

接口管理工具帮助开发者浏览和管理CSM模块的对外接口。

### CSM Interface Browser（接口浏览器）

接口浏览器用于快速浏览和查看项目中所有CSM模块的API接口。

**主要功能**：
- **模块列表**: 显示项目中的所有CSM模块
- **接口展示**: 列出每个模块的所有公开API
- **接口搜索**: 快速查找特定的API
- **参数说明**: 显示API的参数和返回值信息
- **文档链接**: 提供API的详细文档链接

**适用场景**：
- 了解可用的模块和接口
- 查找特定功能的API
- 生成接口文档
- 进行接口设计评审

**使用技巧**：
- 使用搜索功能快速定位API
- 查看接口的参数要求，避免调用错误
- 作为团队的接口规范参考
- 集成到文档生成流程

## 示例与帮助工具

### CSM Example Browser（示例浏览器）

示例浏览器提供了一个集中的界面来浏览和打开CSM的示例项目。

**主要功能**：
- **分类浏览**: 按照功能分类组织示例
- **搜索功能**: 快速查找相关示例
- **描述信息**: 显示每个示例的功能说明
- **快速打开**: 直接打开示例代码

**示例分类**：
1. **基础示例**: 创建和调用CSM模块的基本方法
2. **高级示例**: 工作者模式、责任链模式等高级特性
3. **通讯示例**: 同步调用、异步调用、状态订阅
4. **插件示例**: 各类Addon的使用示例
5. **工具示例**: 全局日志等工具的使用示例

**适用场景**：
- 学习CSM的使用方法
- 寻找代码参考
- 快速原型开发
- 验证功能实现

## JKISM State Editor

JKISM State Editor是JKI State Machine自带的状态编辑工具，CSM完全兼容这个工具。

**主要功能**：
- **状态快速输入**: 通过右键菜单快速添加状态
- **状态模板**: 提供常用的状态模板
- **状态历史**: 记录最近使用的状态
- **快捷键支持**: 提高输入效率

**使用方法**：
1. 在状态字符串控件上右键
2. 选择"Edit State..."
3. 在弹出的编辑器中输入或选择状态
4. 确认后自动填入控件

**与CSM的集成**：
- 完全支持CSM的消息格式
- 支持CSM的特殊符号（-@, ->, ->|等）
- 保存CSM消息历史
- 可以快速切换常用消息

**提高效率的技巧**：
- 使用模板快速生成消息
- 利用历史记录避免重复输入
- 设置常用消息为收藏
- 使用快捷键操作

## 工具开发指南

基于CSM全局日志系统，开发者可以创建自己的调试工具。

### 开发流程

1. **获取全局日志**: 使用`CSM - Global Log Queue.vi`或`CSM - Global Log Event.vi`
2. **处理日志数据**: 解析日志内容，提取需要的信息
3. **实现业务逻辑**: 根据需求实现特定的功能
4. **显示或保存**: 将处理结果显示在界面或保存到文件

### 常见工具类型

- **监控工具**: 实时显示系统状态
- **分析工具**: 统计和分析运行数据
- **调试工具**: 辅助问题诊断
- **记录工具**: 保存运行日志和数据

### 参考示例

- 全局日志队列监控循环: `template/CSM - Global Log Queue Monitoring Loop.vi`
- 全局日志事件监控循环: `template/CSM - Global Log Event Monitoring Loop.vi`
- 各类调试工具的源代码

### 最佳实践

- 使用队列方式获取日志，提高效率
- 实现日志过滤，减少不必要的处理
- 提供友好的用户界面
- 考虑性能影响，避免影响被监控系统
- 提供日志导出功能

## 工具使用注意事项

### 性能影响

- 运行时调试工具会增加系统开销，建议仅在需要时使用
- 大量日志会占用内存，注意控制日志缓存大小
- 在生产环境中，使用源端过滤减少日志产生
- 关闭不需要的调试工具以释放资源

### 安全考虑

- 调试工具可以查看和修改系统状态，注意访问控制
- 日志中可能包含敏感信息，注意保护
- 在生产环境中谨慎使用调试控制台

### 兼容性

- 确保调试工具版本与CSM框架版本兼容
- 某些工具可能需要特定的LabVIEW版本
- 在不同操作系统上测试工具兼容性

## 总结

CSM提供的丰富工具集覆盖了开发的各个阶段，从代码编写、实时调试到接口管理，大大提高了基于CSM开发的效率和质量。合理使用这些工具，可以：

- 快速定位和解决问题
- 深入理解系统运行机制  
- 提高代码开发效率
- 改善代码质量和可维护性

建议开发者根据项目需求，选择合适的工具组合，并根据实际情况定制或开发新的工具。

> 📝 **工具在持续完善中**
>
> CSM的工具还在不断增强和完善中，欢迎社区贡献新的工具和改进！
>
> 关于如何参与CSM的开发，请参考[如何参与贡献](https://nevstop-lab.github.io/CSM-Wiki/how-to-contribute)
